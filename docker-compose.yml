version: '3'

services:
  react-waldo-portfolio:
    image: react-waldo-portfolio
    build: .
    container_name: react-waldo-portfolio
    stdin_open: true
    restart: always
    ports:
      - "3003:3000"

